{
  // Must match your existing Worker's name so deploys UPDATE it in place
  // (and keep the chordrush.app custom domain attached).
  "name": "chordrush",
  "compatibility_date": "2026-07-18",
  // Serve the static site from ./public — no server-side code needed.
  "assets": {
    "directory": "./public"
  }
}
